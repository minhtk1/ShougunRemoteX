# Core dependencies - Updated for Python 3.11.9
pythonnet>=3.0.3; sys_platform == 'win32'
psutil>=7.1.2
pydantic>=2.5.2
loguru>=0.7.2
pyyaml>=6.0.1
watchdog>=3.0.0

# Platform-specific dependencies
pywin32>=306; sys_platform == 'win32'

# Development dependencies
pytest>=7.4.3
pytest-cov>=4.1.0
black>=23.11.0
flake8>=6.1.0
mypy>=1.7.1

# Build dependencies
pyinstaller>=6.16.0
auto-py-to-exe>=2.40.0

# Optional: For advanced features
asyncio-mqtt>=0.16.1
redis>=5.0.1

# Xpra client dependencies (optional for basic functionality)
Pillow>=9.0.0
# rencode>=1.0.8

# GUI dependencies - Xpra client cần PyGObject cho GTK3 backend
# LƯU Ý: Trên Windows, cần cài đặt PyGObject qua MSYS2 hoặc từ pre-built wheels
# Nếu không có MSYS2, có thể cần sử dụng Qt6 backend thay vì GTK3
# PyGObject>=3.40.0; sys_platform == 'win32'
# pycairo>=1.20.0; sys_platform == 'win32'